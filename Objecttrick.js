
var x = 10;
var obj = {
        name: "Saiful",
        address: "Dhupkhola, Gendaria",
        x,

}
// to find the all key use Object.keys(obj_name)
var keys = Object.keys(obj);
console.log(keys);

//to find all object's value use Object.values(obj_name)
var values = Object.values(obj);
console.log(values);

//to find all entires of the object use Object.entires(obj_name)
var entries = Object.entries(obj);
console.log(entries);

//entries[0][1] = 'Ratul';
console.log(entries);

